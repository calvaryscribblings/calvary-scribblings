Recovered verbatim from `stash@{0}^3` (blob 0770015) on 2026-07-28 — byte-identical, unmodified.

Pre-adaptation: still writes the old Dead End paywall node `purchases/{uid}/{slug}` and hardcodes `DEFAULT_SLUG = 'dead-end-a-halfway-around-the-moon-story'`; our node is `bookstore_purchases/{uid}/{titleId}`.

**The port landed in R5.** Live code is `functions/api/bookstore/stripe-webhook.js`; this directory is now archival only and is not deployed. The port kept the signature verification, PEM normalisation, service-account OAuth minting and the 200-after-verification response policy verbatim, and changed: the target node (`bookstore_purchases/{uid}/{titleId}`), the identity fields (`client_reference_id` / `metadata.titleId`, no Dead End default), the RTDB auth style (`Authorization` header rather than `?access_token=`), plus new denormalised display fields, an idempotency guard, and refund/dispute revocation.

Do not edit this file expecting it to run. Change `functions/api/bookstore/stripe-webhook.js`.
