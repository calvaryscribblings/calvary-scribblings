{
  "_comment": "SIGNUP COMPLETENESS (24 Sep 2026) — DEPLOYED IN W1, same day. The STRICT handle-integrity rule, tested by tests/rules/handle-strict.test.mjs, now live on users/$uid/{handle,handleLowercased,username} in database.rules.json. It was held back while the app's write order was unknown; the app session then proved every binary (the 1.4.0 line, builds 32 and 34, the current OTA) writes the claim in the same update as the profile (rules probe 176/176). This file stays as the reference the test compares the live rule against.",
  "users/$uid/handle/.validate": "newData.isString() && (newData.val() === '' || newData.parent().parent().parent().child('usernames').child(newData.val().toLowerCase()).val() === $uid)",
  "users/$uid/handleLowercased/.validate": "newData.isString() && (newData.val() === '' || newData.parent().parent().parent().child('usernames').child(newData.val().toLowerCase()).val() === $uid)",
  "users/$uid/username/.validate": "newData.isString() && (newData.val() === '' || newData.parent().parent().parent().child('usernames').child(newData.val().toLowerCase()).val() === $uid)"
}
